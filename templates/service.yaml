apiVersion: v1
kind: Service
metadata:
  name: {{ include "docux-chart.fullname" . }}
  labels:
    {{- include "docux-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "docux-chart.selectorLabels" . | nindent 4 }}
